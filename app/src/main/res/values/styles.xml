<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="AppTheme.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="bottomSheetDialogTheme">@style/AppBottomSheetDialogTheme</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:colorBackground">@android:color/white</item>
        <item name="android:windowBackground">@android:color/white</item>
    </style>

    <style name="CenterProgress" parent="Widget.AppCompat.ProgressBar">
        <item name="android:indeterminate">false</item>
        <item name="android:color">@color/colorPrimaryDark</item>
        <item name="android:progressTint">@color/colorPrimary</item>
        <item name="colorAccent">@android:color/darker_gray</item>
    </style>

    <style name="AppBottomSheetDialogTheme" parent="Theme.Design.Light.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/BottomDialog</item>
    </style>

    <style name="BottomDialog" parent="Widget.Design.BottomSheet.Modal">
        <item name="android:background">@drawable/bg_dialog_bottom</item>
    </style>

    <style name="AppTheme.NoActionBar.Full" parent="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="splashTheme" parent="AppTheme.NoActionBar.Full">
        <item name="android:windowBackground">@drawable/bg_gradient_color</item>
    </style>

</resources>
